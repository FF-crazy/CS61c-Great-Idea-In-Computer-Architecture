jal ra, 4  # 这里需要是4的倍数，只是在转化为二进制的时候会自动省略末尾的0
addi t0, zero, 100
addi t0, zero, 101
addi t0, zero, 102
addi t0, zero, 103
jal ra, 4
addi t0, zero, 104
addi t0, zero, 105
addi t0, zero, 106
addi t0, zero, 107